"use client";import Link from "next/link";import {Product,money} from "../data";export default function ProductCard({p}:{p:Product}){return <article className="productCard"><div className="productImage">{p.badge&&<small>{p.badge}</small>}<button className="wish" aria-label="Add to wishlist">♡</button><Link href={`/product/${p.slug}`}><img src={p.image} alt={p.name}/><span>View details</span></Link></div><Link href={`/product/${p.slug}`}><h3>{p.name}</h3></Link><p>{money(p.price)}</p></article>}
